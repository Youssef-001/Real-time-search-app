// example

// loop on every index in your result array and highlight with highlightText method

private void highlightText(int startIndex, int length)
{
textBox.Select(startIndex, length);

textBox.SelectionBackColor = Color.Yellow;  
textBox.SelectionForeColor = Color.Black;  

}